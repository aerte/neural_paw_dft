"""Shared library for VASP SCF runners.

Behavior is lifted verbatim from the legacy per-variant scripts so that the
unified `run_mp.py` / `run_gnome.py` entry points produce identical CSVs
and output directories.
"""
