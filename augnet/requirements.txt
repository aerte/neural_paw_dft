# Requires Python >=3.10 (cuequivariance-torch supports 3.10-3.14)
# Install torch first, matched to your CUDA build:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121

torch>=1.12
mace-torch==0.3.16       # upstream https://github.com/ACEsuit/mace (was vendored)
e3nn==0.4.4              # pinned by MACE
numpy
scipy
opt-einsum-fx           # import: opt_einsum_fx
ase
pymatgen                # CHGCAR parsing
matscipy                # neighbour lists
h5py
lz4                     # import: lz4.frame
PyYAML                  # import: yaml
torch-ema               # import: torch_ema
torchmetrics
prettytable
tqdm
lmdb                    # MACE data backend
python-hostlist         # import: hostlist (MACE tools)
configargparse          # MACE tools

# Training / logging
lightning               # import: lightning.pytorch (PyTorch Lightning 2.x)
wandb                   # experiment tracking

# CUDA-accelerated equivariance
cuequivariance-torch>=0.2.0    # import: cuequivariance, cuequivariance_torch
# OpenEquivariance: open-source fused CUDA/HIP kernel for the convolution tensor
# product (portable NVIDIA Ampere+ / AMD). Enable via model.enable_oeq. JIT-
# compiles kernels at first use, so needs a host C++/CUDA toolchain (gcc>=9,
# nvcc) available at runtime. Requires torch>=2.7.
openequivariance>=0.6.0        # import: openequivariance
